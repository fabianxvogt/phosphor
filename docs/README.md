# Phosphor documentation

This folder keeps the release evidence concise and searchable.

- `README.md` — this navigation page.
- `../README.md` — user-facing quickstart, bounds, and privacy behavior.
- `../ROADMAP.md` — Now / Next / Later / Done and the honest verification state.

The governing creative brief is maintained outside this repository at `Development/docs/projects/GAMES_VISUALS_CREATIVE.md`. The current source is classified `INCREMENTAL / PREVIEW`: all ten bounded scene families are registered in one shared instrument and the new families have concrete source mechanisms, while independent review, browser/device measurements, a sustained rehearsal, and human aesthetic/performer review remain unverified.

Correctness evidence includes deterministic seed RNG and cellular fixtures, bounded reaction-diffusion, feedback, ecology, topology, and coupled-phase checks, truthful quality profiles with immediate output resize/cadence/work-budget coverage, Cathedral shading-oracle coverage, local Aquarium food consumption/starvation coverage, beat-timed Magnetic gesture pause/stop coverage, fixed-cap source contract checks for all ten families, a 900-frame bounded renderer stress/replay check across all ten families, a ten-switch lifecycle fixture, strict nested import bounds, real scene55 arc-control/cue/rehearsal coverage, ten-family library labels/starting looks, Explore/Perform persistence, and prior Chromium fresh-session checks. These new depth paths are source-checked but not browser-observed in this continuation; actual runtime performance, visual material distinction, and pointer/audio feel remain open browser/human gates.

The shared import path validates and sanitizes a complete candidate before committing state, migrates valid three-scene `phosphor-set-v1` saves by filling new defaults, applies authored preset payloads during cue transitions, scopes gestures to the active scene, records bounded Phase setup/events, archives one explicitly captured Phase measurement with a versioned identity and event position, and records child parameter snapshots for Evolution promotion. Reopen/import resets live Phase buffers but preserves the validated archived snapshot; the compact native inspector shows live current and saved capture separately. Phase exports carry both and explicitly do not claim a lossless mid-buffer checkpoint. Context-loss recovery resets the active bounded renderer after restoration. These are source behaviors only until observed in a browser.

The experience layer keeps the canvas as the primary surface, makes the ten-family library browsable without replacing renderer output with thumbnails, and exposes progressive controls without removing the full Explore path. Focus canvas is a local layout mode, not a browser fullscreen or deployment claim. Scene action hints now match each family, including Evolution sibling selection. Narrow screens use a shorter stage and sticky pause/play/save/export quick bar. Quality labels are truthful output targets, not observed performance. Cathedral treatments are bounded shading inputs, Aquarium feeding is an artistic Lenia-inspired energy toy rather than a biological model, and Magnetic replay follows the shared tempo/pause clock only for timed events.

Scene55 migration note: older unpublished preview saves may contain a `phase.model` field from the former three-branch experiment. Validation ignores that field and applies the fixed Coupled Regime Field defaults; no legacy model choice is replayed.
